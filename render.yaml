services:
  - type: web
    name: chess-lobby-server
    runtime: node
    buildCommand: npm install --prefix apps/server && npm run build --prefix apps/server
    startCommand: npm start --prefix apps/server
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
